# ToDone

**从长期 Todo 到 Done 的转变** —— 不只管理待办，而是记录每一次"已完成"：个人时间记录 + 番茄钟台账，沉淀复盘与成长数据。

| 端 | 技术栈 | 目录 |
|---|---|---|
| 桌面端（Windows / macOS 规划中） | Tauri 2 · React 18 · TypeScript · Vite 5 · SQLite | `donelist/` |
| 移动端（Android） | Flutter · Dart 3 · sqflite | `mobile/` |

---

## 功能特性

### 桌面端（`donelist/`）

- **今日页**：快捷录入、按"昨天 / 今天 / 明天 + 自选日期"切换查看，历史日期可编辑
- **每日时间线**：以三色块展示一天的时间分布
  - **专注**（青色渐变块，对应"记录"）· **休息**（粉橙渐变块）· **虚度**（灰色块，不显眼）
  - 数据不直接写在块内，鼠标靠近唤出对应时间段做了什么事
  - 时间冲突规则：**记录优先**——记录一旦创建，外界任何操作（休息/虚度落块等）都不会改动记录数据，不做拆分
  - 休息 / 虚度在每日记录页有独立小入口，只需填时间段 + 标题
- **番茄钟**：专注 / 休息双模式，完全对应的两种视觉（青绿渐变 / 粉橙渐变），运行中锁定切换，结束彩带 / 涟漪庆祝；完成后自动生成"专注"记录；输入框回车即可开始
- **标签 / 洞察 / 年度图表**：按领域、来源、难度、精力聚合复盘
- **中英双语**：设置页一键切换（163+ 词条），全站文案覆盖
- **数据安全**：SQLite 主存储 + localStorage 自动降级，自动备份（60s 防抖，保留 10 份）、手动导出 / 导入（JSZip）
- **手机数据导入**：从移动端导出的 JSON 增量合并（按 id + updated_at，较新者胜出）

### 移动端（`mobile/`，Android）

- **番茄钟**：完整复刻桌面端状态机（专注 / 休息、预设、进度圆环、完成庆祝），计时状态持久化，切页 / 退后台不中断
- **今日记录**：简洁条目列表（无时间线）+ 昨天 / 今天 / 明天切换 + 2×2 统计卡，支持编辑 / 删除 / 精力与四维标签
- **设置**：中英切换；**数据导出**（JSON 到剪贴板，格式与桌面端一致，可被 PC 设置页增量导入）
- 数据层复刻桌面端 SQLite 表结构与时间冲突约束（重叠 ABORT 触发器）

---

## 技术栈

| 层面 | 桌面端 | 移动端 |
|---|---|---|
| UI | React 18.3 + TypeScript 5.5（Hooks） | Flutter / Dart 3.13 |
| 构建 | Vite 5（dev 端口 1420，HashRouter） | Gradle（Android） |
| 桌面壳 | Tauri 2（Rust） | — |
| 数据库 | tauri-plugin-sql（SQLite `donelist.db`） | sqflite（同构表结构） |
| 图表 | Recharts 3 | 自绘（CustomPaint） |
| 备份 / 同步 | JSZip 打包 · mobileSync 增量导入 | JSON 导出到剪贴板 |
| 国际化 | `src/i18n.ts`（zh / en） | `lib/i18n/strings_{zh,en}.dart` |

---

## 未来路线图（规划中）

- [ ] **macOS 适配**：桌面端（Tauri）在 Mac 上构建（代码已跨平台，需 macOS 环境 + Xcode 工具链，bundle 补 .icns 配置）
- [ ] **双机数据同步**：桌面备份 JSON 文件走同步盘（OneDrive / 坚果云 / iCloud），扩展导出 / 导入以包含时间线（time_slots），实现 Windows ↔ Mac 增量合并
- [ ] 移动端构建产物发布（release 签名 APK）
- [ ] 版本号三处对齐（package.json / Cargo.toml / APK 命名）
